{
  "name": "inksnap-mockup-proxy",
  "version": "1.0.0",
  "description": "Simple proxy server to call an image-edit API for mockup generation (stores API key server-side).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5",
    "node-fetch": "^2.6.7",
    "form-data": "^4.0.0"
  }
}
